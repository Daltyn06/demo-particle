{#
/**
 * @file
 * slider-sync
 *
 * Available variables for slider-sync:
 *  - slider_sync.variable_name [variable_type]: Description of the variable. Example below.
 *  - slider_sync.invert [bool]: When set, colors will be inverted for light display on dark background.
 */
#}

<div class="slider-sync">
  <div class="featured">
    {% block image_main %}
    {% for image in thumbnail_list %}
      {% include '@atoms/image/_image.twig' with {
        image: image.image
      } %}
    {% endfor %}
    {% endblock image_main %}
  </div>
  <div class="thumbnails">
    {% block image_thumb %}
    {% for image in thumbnail_list %}
      <div class="image-wrapper">
        {% include '@atoms/image/_image.twig' with {
          image: image.image
        } %}
      </div>
    {% endfor %}
    {% endblock image_thumb %}
  </div>
</div>
